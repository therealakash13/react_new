export default function Para1() {
  return (
    <p>
      A React component is a reusable, self-contained piece of UI that defines
      how a part of the interface looks and behaves. Components let you split an
      application into small, manageable building blocks—such as buttons, forms,
      cards, or entire pages—each responsible for its own structure, logic, and
      styling. In modern React, components are usually written as functions that
      return JSX, a syntax that looks like HTML but lets you embed JavaScript
      directly into your UI.
    </p>
  );
}
